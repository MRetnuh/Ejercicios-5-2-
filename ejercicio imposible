#include <stdio.h>
#include <string.h>

int main(int argc, char *argv[]) {
    char c[3];
    puts("¿Tienes crédito? ");
    scanf("%s", c); 

    if(strcmp(c, "si") == 0) { 
        printf("Hola estimado\n");
    } else {
        printf("Consígate, gil\n");
    }
    return 0;
}


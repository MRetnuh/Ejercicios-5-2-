#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[]){
    int n1;
    int n2;
    puts("ingrese dos numeros: ");
    scanf("%d", &n1);
    scanf("%d", &n2);
/*
Se establecen las variables n1 y n2 (el primero debe ser el menor y el segundo el mayor ya que son los limites), 
cuyos valores los tiene que ingresar el usuario
*/
    while(n2 > n1){
        if(n2 % 2 == 0){
            printf("%d ", n2);
        }
        n2--;
    }
    printf("\n");
    return 0;
/*
Teniendo los valores, se entra en un bucle que perdurara hasta que n2 sea menor a n1. Mientras funcione, el valor de n2 desciende uno en uno y si uno 
de estos valores es par, se imprime
*/
}

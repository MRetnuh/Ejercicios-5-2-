#include <stdio.h>

int main(int argc, char *argv[]){
int numero;
puts("Ingrese un numero: ");
scanf("%d", &numero);
if(numero > 0){
printf("El valor absoluto de +%d es %d\n", numero, numero);
}
else{
    printf("El valor absoluto de %d es %d\n", numero, numero * -1);
}
    return 0;
    }

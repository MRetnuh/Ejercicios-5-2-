#include <stdio.h>

int main(){
int a = 0;
int b = 0;
int c = 0;
while(a <= 87){
    a++;
    b = a - 1;
    c = b - 1;
    if(a + b + c == 87){
        printf("los números consecutivos que dan 87 son %d, %d y %d\n", c, b, a);
    }
}
    return 0;
}

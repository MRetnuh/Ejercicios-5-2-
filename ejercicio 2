#include<stdio.h>

int main(){
	int a;
	int b;
	int c;
	puts("Ingrese 3 numeros: ");
	scanf("%d %d %d", &a, &b, &c);
	double promedio = (a + b + c) / 3;
	printf("El promedio de los numeros ingresados es: %.2f\n", promedio);
	return 0;
	
}
